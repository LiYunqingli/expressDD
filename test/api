# 消息接口文档

## 1. 获取消息接口（get.php）

### 接口功能
用于指定指定指定指定指定用户获取最新消息，仅返回指定时间之后的未读消息，并将这些消息标记为已读。

### 请求地址
`https://kd.lihuarong.cn:8080/test/php/get.php`

### 请求方法
POST

### 请求头
`Content-Type: application/x-www-form-urlencoded`

### 请求参数
| 参数名 | 类型   | 是否必填 | 说明                 |
|--------|--------|----------|----------------------|
| time   | string | 是       | 最后一次获取消息的时间 |
| user   | string | 是       | 要获取消息的用户名   |

### 响应数据
- 有新消息响应
```json
{
  "code": 200,
  "msg": "have new msg",
  "data": [
    {
      "id": "消息ID",
      "user": "接收消息的用户",
      "msg": "消息内容",
      "time": "消息发送时间"
    },
    // ...更多消息
  ]
}
```
- 无新消息响应
```json
{
  "code": 201,
  "msg": "0 lines",
  "data": []
}
```

### 响应说明
- `code=200`：存在新消息，`data`数组中包含具体消息列表
- `code=201`：不存在新消息，`data`为空数组
- 消息数据字段说明：
  - `id`：消息唯一标识
  - `user`：消息接收用户
  - `msg`：消息具体内容
  - `time`：消息发送时间戳