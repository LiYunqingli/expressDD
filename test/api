# 接口API文档

## 基础信息
- 主机地址：`https://kd.lihuarong.cn:8080/test/`


## 1. 获取消息接口（get.php）

### 接口地址
`https://kd.lihuarong.cn:8080/test/php/get.php`

### 请求方式
`POST`

### 请求头
| 头部名称 | 取值 | 说明 |
|---------|------|------|
| Content-Type | application/x-www-form-urlencoded | 表单提交格式 |

### 请求参数
| 参数名 | 类型 | 是否必填 | 说明 |
|-------|------|----------|------|
| time | string | 是 | 时间字符串，格式为`YYYY-MM-DD HH:MM:SS`，用于获取该时间之后的消息 |

### 响应数据
- 数据格式：`JSON`
- 成功响应（有新消息）：
```json
{
  "code": 200,
  "msg": "have new msg",
  "data": [
    {
      "id": 1,
      "msg": "消息内容",
      "time": "2024-05-20 12:00:00"
    },
    {
      "id": 2,
      "msg": "另一条消息",
      "time": "2024-05-20 12:01:00"
    }
  ]
}
```
- 成功响应（无新消息）：
```json
{
  "code": 201,
  "msg": "0 lines",
  "data": []
}
```

### 字段说明
- `code`：状态码（200表示有新消息，201表示无新消息）
- `msg`：状态描述信息
- `data`：消息数组，包含id（消息ID）、msg（消息内容）、time（消息时间）


## 2. 发送消息接口（send.php）

### 接口地址
`https://kd.lihuarong.cn:8080/test/php/send.php`

### 请求方式
`POST`

### 请求头
| 头部名称 | 取值 | 说明 |
|---------|------|------|
| Content-Type | application/x-www-form-urlencoded | 表单提交格式 |

### 请求参数
| 参数名 | 类型 | 是否必填 | 说明 |
|-------|------|----------|------|
| text | string | 是 | 要发送的消息内容，不可为空 |

### 响应数据
- 数据格式：`JSON`
- 发送成功：
```json
{
  "code": 200,
  "msg": "success"
}
```
- 发送失败：
```json
{
  "code": 500,
  "msg": "error"
}
```

### 字段说明
- `code`：状态码（200表示成功，500表示失败）
- `msg`：状态描述信息